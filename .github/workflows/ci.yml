name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test with race detector
        run: go test -race -timeout 120s ./...

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    env:
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_KEY: ${{ secrets.SSH_KEY }}
    steps:
      - name: Check deploy configuration
        id: deploy_config
        run: |
          if [ -z "${SSH_HOST}" ] || [ -z "${SSH_USER}" ] || [ -z "${SSH_KEY}" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "Deployment secrets are not configured; skipping deploy."
            exit 0
          fi

          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: Deploy to server
        if: steps.deploy_config.outputs.enabled == 'true'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ env.SSH_KEY }}
          command_timeout: 20m
          script: |
            cd /opt/xleaks
            git pull --ff-only origin main
            make build
            install -m 0755 bin/xleaks /usr/local/bin/xleaks.new
            mv /usr/local/bin/xleaks.new /usr/local/bin/xleaks
            systemctl restart xleaks-node
            systemctl is-active --quiet xleaks-node
