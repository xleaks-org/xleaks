# Bootstrap peers for initial network connectivity.
# Entries must be full libp2p multiaddrs, for example:
#
# [[peers]]
# address = "/dnsaddr/bootstrap.example.org/tcp/7460/p2p/12D3KooW..."
#
# This file intentionally ships without active defaults until valid peer IDs
# are known.
